@model IEnumerable<A15147442_ENT.Entities.Distrito>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Distritos</h2>

<p>
    @Html.ActionLink("CREAR", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.DistritoId }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.DistritoId }) |
            @Html.ActionLink("Borrar", "Delete", new { id=item.DistritoId })
        </td>
    </tr>
}

</table>
