@model IEnumerable<A15147442_ENT.Entities.CentroDeAtencion>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("AGREGAR", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Centro)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Centro)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.CentroId }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.CentroId }) |
            @Html.ActionLink("Borrar", "Delete", new { id=item.CentroId })
        </td>
    </tr>
}

</table>
