@model IEnumerable<A15147442_ENT.Entities.LineaTelefonica>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Lineas Telefonicas</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TipoDeLinea)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TipoDeLinea)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.LineaId }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.LineaId }) |
            @Html.ActionLink("Borrar", "Delete", new { id=item.LineaId })
        </td>
    </tr>
}

</table>
