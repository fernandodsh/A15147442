@model IEnumerable<A15147442_ENT.Entities.CentroDeAtencion>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Centro De Atenciones", "New")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Centro)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Centro)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CentroId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CentroId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CentroId })
        </td>
    </tr>
}

</table>
